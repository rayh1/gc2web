CR: specs/website-beauty/website-beauty.cr.md
Spec-sha: f24e05ca1f0f
Built-at: 2026-08-22T00:14:04+02:00

[R-1] MET — pre-existing — shared warm archive system: `archive` palette + `body`/`display` font stacks in tailwind.config.cjs; `page-shell`/`content-frame`/`btn` utilities and dark re-keying (`dark:*-archive-dark-*`) in src/styles/global.css; consumed by src/layouts/PageLayout.astro, src/layouts/BlogPostLayout.astro, src/components/Header.astro, src/pages/index.astro, src/pages/search.astro. The visual-QA AC is review-only and remains for the reviewer. — test: tests/e2e/visual-refresh.spec.ts::"home|search|entity browse|entity detail route uses the shared archive palette and typography" (coverage built this pass)

[R-2] MET — built — the homepage itself pre-existed and satisfies the requirement (purpose paragraph, `/search` and `/entity` actions in src/pages/index.astro); what was missing/red was the e2e side: tests/e2e/homepage.spec.ts was stale (expected copy /een warme ingang/i the page never rendered) and asserted no viewports. Rewritten against the shipped copy with first-viewport assertions (toBeInViewport at scrollY=0) at 1280x720 and 375x667; geometry pre-verified against the built page at both sizes. — test: tests/e2e/homepage.spec.ts::"homepage shows purpose and both primary actions in the first desktop viewport" and "...first mobile viewport"

[R-3] MET — built — grouping itself is pre-existing and generator-backed (frontmatter `section_order`/metadata from gen_site; sectionMeta mapping and conditional group rendering in src/layouts/BlogPostLayout.astro). The dense-entity e2e pre-existed (i00005); the AC's sparse-entity coverage was missing and was built: s00001 (title-only optional metadata) asserting empty groups are omitted, not rendered empty. — test: tests/e2e/entity-page.spec.ts::"sparse entity page renders intentionally with empty groups omitted" and "dense entity page surfaces grouped overview content"

[R-4] MET — pre-existing — unified result card src/components/PostListItem.tsx used by browse and search; metadata and fallback paths covered by component tests. — test: src/components/__tests__/PostListItem.test.tsx::"renders contextual metadata when available" and "falls back to title and id when metadata is missing"

[R-5] MET — built — navigation/breadcrumbs pre-existing (src/components/Header.astro sticky header + mobile menu, src/components/Breadcrumbs.astro, `currentContext` from both layouts and src/pages/entity/[...page].astro). The AC's desktop+mobile per-page-type assertions were not present; built as a 2-viewport x 3-page-type matrix asserting breadcrumb presence/content, home link in the trail, and search reachability (header link on desktop, opened mobile menu on mobile). — test: tests/e2e/navigation.spec.ts::"browse|search|entity page keeps home and search reachable with breadcrumb context (desktop|mobile)"

[R-6] MET — pre-existing — focus-visible rule for a/button/input/summary at src/styles/global.css:129-134 using the archive accent outline; primary content is static Astro output (heading and both actions grep-able in dist/index.html, gate E3; no client-render gate on primary content). — test: none — verified by mechanical gates E3 (static-HTML grep) and E8 (focus-visible grep); no behavioural test adds substance over those checks

[R-7] MET — pre-existing — family-branch entry points on the homepage (cards linking /entity/i00005/ and /entity/i00073/ in src/pages/index.astro) plus shared nav routes to browse/search. — test: tests/e2e/homepage.spec.ts::"homepage keeps the family-branch entry points reachable" (assertion built this pass)

[R-8] MET — built — created tests/e2e/visual-refresh.spec.ts asserting the shared system across home, search, entity browse and entity detail routes: `page-shell` on body plus computed background/text color and body/heading font families equal to the shared token values on every route (values pre-verified against the built site). Both locked layers run green on the current tree: `npm run test:unit` exit 0 (6 tests) and `npm run test:e2e` exit 0 (26 tests). — test: tests/e2e/visual-refresh.spec.ts::"home|search|entity browse|entity detail route uses the shared archive palette and typography"

Delegated decisions taken:
- E4/E10 direction: the shipped homepage copy stands; the stale test bends. Measured against the built page first: heading, purpose paragraph and both actions sit fully inside the first viewport at 1280x720 and 375x667, so [R-2]'s ACs hold with no page change. The test was asserting copy ("een warme ingang") that was never a contract — exact copy is Delegated by the spec.
- Viewport sizes for "a desktop and a mobile viewport": 1280x720 and 375x667.
- Visual-refresh cohesion assertion: exact computed values (light scheme) of the archive tokens — canvas rgb(243,236,226), ink rgb(47,36,29), IBM Plex Sans body stack, Iowan Old Style display stack — asserted identically on all four routes, plus the shared `page-shell` class; chosen over a classes-only check so an off-system route fails even if it copies class names.
- Sparse-entity fixture: s00001 (source entity, no optional frontmatter beyond description/pubDate).

Declined as out of scope: no changes to src/ (pages, layouts, components, styles) — the shipped implementation already satisfies the functional side of every requirement; only test files were touched. No changes to the gen_site pipeline, no dark-mode e2e matrix (the spec's dark AC is carried by gate E2 and the token structure), no gates/spec edits.

Verification of this pass: `npx playwright test` green per spec file and as a full suite (26 passed); `npm run test:unit` exit 0. Full-pass runner: `python3 /workspace/.claude/skills/cr4/scripts/verify.py --project .` refused suite detection verbatim — "more than one test system detected (uv run pytest, npm test); running one would leave the other unrun and report clean. Pass --tests explicitly, repeating it per suite." — and was re-run as instructed with the CR's two locked suites (`--tests "npm run test:unit" --tests "npm run test:e2e"`); result: `"verdict": "clean"` — tests lane clean (both commands exit 0; "no JUnit report; status is from exit codes only"), constitution lane clean (1 PASS). Summary at .verify/summary.json. The detected `uv run pytest` suite belongs to the gen_site pipeline, which this CR's scope fence excludes from rework and whose layer [R-8] does not lock; it was not run in this pass.
