---
import type { CollectionEntry } from "astro:content";
import Post from "./Post.astro";

interface Props {
  posts: CollectionEntry<"entity">[];
};

const { posts } = Astro.props;
---
<ul class="not-prose grid list-none gap-4 pl-0 lg:grid-cols-2">
  {
    posts.map((post) => {
      return (
        <li>
          <Post post={post} />
        </li>
      );
    })
  }
</ul>
