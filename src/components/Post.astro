---
import type { CollectionEntry } from "astro:content";
import { PostListItem } from "./PostListItem";

interface Props {
  post: CollectionEntry<"entity">;
};

const { post } = Astro.props;
const { data } = post;

const finalPost: CollectionEntry<"entity"> = {
  ...post,
  data: {
    ...data,
  },
};
---

<PostListItem post={finalPost} />
